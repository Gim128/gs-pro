import React from 'react';

const OutletAdminContent = () => {
    return (
        <div>
            this is outlet admin content and you only have outlet admin privileges
        </div>
    );
};

export default OutletAdminContent;
