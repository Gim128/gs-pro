import React from 'react';

const UserContent = () => {
    return (
        <div>
            this is user content and you only have user privileges
            </div>
    );
};

export default UserContent;
